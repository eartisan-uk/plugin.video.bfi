v1.5.9
- Fix watchlist listing: restore div.card / a.card__action parser (watchlist page never
  used film-card elements); v1.5.7 misidentified the HTML structure and removed the
  correct parse_card() helper — restored here

v1.5.8
- Fix watchlist: detect expired BFI session (site returns HTTP 200 with login page when
  cookies expire); auto-clear stale session and prompt re-login with "Session expired"
  notification

v1.5.7
- Fix watchlist card parsing: update selector to match BFI site redesign (film-card
  elements); watchlist now correctly lists films

v1.5.6
- Fix detail screen: trailer list item now shows a distinct title and no duration;
  skin-assigned title is film title for Watch Now, film title + " - Trailer" for Watch Trailer

v1.5.5
- Film detail screen: clicking a title opens Watch Now / Watch Trailer / Add to Watchlist
  sub-menu before playing; full metadata (plot, cast, director, country, certificate)
  scraped from BFI film page; trailers and watchlist require BFI+ subscription

v1.5.4
- Fix autoplay: clear video playlist before resolving stream so Kodi cannot advance to
  next item; bump version to 1.5.4

v1.5.3
- Fix autoplay: set isFolder=False on playable items so Kodi stops queuing siblings
- Add aspect ratio stream hint for correct display on FireTV and other media devices
- Bump version to 1.5.3

v1.5.2
- Fix Collections (root cause): BS4 coerces None attrs to {'class': None} which
  requires h3/p to have NO class — but BFI's collection cards use class="dark";
  changed JIG collections_overview title/plot to use {} so any h3/p is matched

v1.5.1
- Fix Collections: overview pages use collection-card HTML (not film-card); added
  collections_overview JIG key and updated both sub-menus to pass the correct key
- Updated ku.art() to fall back through data-img-200/285/344 so collection tile
  artwork resolves (portrait images, not the widescreen 800px used by film cards)
- Remove defunct Stream/Bandwidth setting (fed the dead Ooyala fallback URL only;
  has no effect on current Brightcove playback)
- Hide The Cut and BFI Replay from menus (code retained for future use)
- New menu icons: subscription, free, exclusives, recently-added, coming-soon,
  inside-film, shorts (generate via resources/media/make_icons.py)
- Remove unused icons home.png and new.png
- Bump version to 1.5.1

v1.5.0
- UI overhaul: structured top-level menu with Subscription and Free sub-menus
- Subscription sub-menu: Exclusives, Recently Added, Kermode Introduces, Popular,
  Collections, Coming Soon
- Free sub-menu: Popular, Inside Film, Shorts, Collections
- Watchlist: fetches /account/watchlist with auth; parses .card/.card__action
  structure (different from film-card used elsewhere)
- New parse_card() helper for account-page card format
- Auth cookies now auto-attached to /account/ URLs as well as /subscription/
- New localised strings 32038-32045
- Settings Menu category simplified to match new structure
- Bump version to 1.5.0

v1.4.1
- Fix subscriber video playback: correct Brightcove account (6057940601001) and
  MLNFA1L1R DRM player extracted from script tags rather than HTML attributes
- Bump version to 1.4.1

v1.4.0
- BFI+ subscriber login (email/password via Settings > Account)
- Session cookies persisted to special://userdata/addon_data/plugin.video.bfi/session.json
- Login endpoint: POST /user/login?_format=json (Drupal JSON API)
- New auth.py module; login/logout routes in plugin.py
- settings.xml: new Account category with email, password, Sign In, Sign Out
- New localised strings 32027-32037
- Bump version to 1.4.0

v1.3.0
- Kodi 21 (Omega) / Python 3 compatibility fixes
- Bumped xbmc.python requirement to 3.0.0
- Replaced defunct Ooyala player (shut down ~2022) with Brightcove support
- Fixed xbmc.translatePath -> xbmcvfs.translatePath (removed in Kodi 19)
- Fixed xbmc.log UnicodeEncodeError handler (unnecessary in Python 3)
- Bundled cache module (cache.py) — removes dependency on script.module.cache
- Bump version to 1.3.0

v1.2.0
- Updated to use script.module.cache
- Fixes duration when parsing collection items
- Added all genres when parsing collection items
- Added "clear searches" to settings
- Added "Save recently viewed" option to settings
- Added additional short videos to "The Cut" selection
- Added "Kermode Introduces" menu and videos
- Amends recently viewed to show image, description, etc.

v1.1.0
- Python 3 compatible
- Removed hardcoded special URLs

v1.0.3
- Fixes relative image URLs

v1.0.2
- Scraper update to reflect changes on BFI website

v1.0.1
- Small bug fix

v1.0.0
- Initial version
